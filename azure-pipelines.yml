trigger: none

jobs:
- job: TestTrigger
  pool: server   # runs without any agent
  steps:
    - task: PowerShell@2
      inputs:
        targetType: inline
        script: |
          Write-Output "✅ Pipeline triggered from Logic App (server job)"
